<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.labo1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".modification_solde"></activity>
        <activity android:name=".modification_credit" />
        <activity android:name=".liste_comptes_delinquants" />
        <activity android:name=".informations_compte" />
        <activity android:name=".modifier_compte" />
        <activity android:name=".suppression_compte" />
        <activity android:name=".CreationCompte" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>